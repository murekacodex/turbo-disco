<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Choice Shop - Order Form</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
    <header class="flex items-center justify-between p-4 bg-blue-500 text-white shadow-md">
        <h1 class="text-2xl font-bold">Throat-ease Drinks</h1>
        <nav class="flex space-x-6">
            <a href="/" class="hover:text-gray-200 transition duration-300">Order Juices</a>
            <a href="/orders" class="hover:text-gray-200 transition duration-300">View Orders</a>
            <a href="/login" class="hover:text-gray-200 transition duration-300">Login</a>
            <a href="/logout" class="hover:text-gray-200 transition duration-300">Logout</a>
        </nav>
    </header>
    <main>
        <h2 class="text-center p-5 font-bold text-2xl">Order Juices!</h2>
        <form action="/submitOrder" method="POST" class="max-w-lg mx-auto bg-white p-6 rounded-lg shadow-lg space-y-4">
            <h2 class="text-2xl font-semibold text-center mb-6">Order Form</h2>

            <div>
                <label for="name" class="block text-lg font-medium text-gray-700">Name*:</label>
                <input type="text" id="name" name="name" value="<%= data.name || '' %>" required
                    class="w-full p-3 mt-1 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            </div>

            <div>
                <label for="phone" class="block text-lg font-medium text-gray-700">Phone*:</label>
                <input type="text" id="phone" name="phone" value="<%= data.phone || '' %>" required
                    class="w-full p-3 mt-1 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            </div>

            <div>
                <label for="mangoJuices" class="block text-lg font-medium text-gray-700">Mango Juices:</label>
                <input type="number" id="mangoJuices" name="mangoJuices" value="<%= data.mangoJuices || '' %>"
                    class="w-full p-3 mt-1 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            </div>

            <div>
                <label for="berryJuices" class="block text-lg font-medium text-gray-700">Berry Juices:</label>
                <input type="number" id="berryJuices" name="berryJuices" value="<%= data.berryJuices || '' %>"
                    class="w-full p-3 mt-1 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            </div>

            <div>
                <label for="appleJuices" class="block text-lg font-medium text-gray-700">Apple Juices:</label>
                <input type="number" id="appleJuices" name="appleJuices" value="<%= data.appleJuices || '' %>"
                    class="w-full p-3 mt-1 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            </div>

            <div>
                <button type="submit"
                    class="w-full p-3 bg-blue-500 text-white rounded-md hover:bg-blue-600 transition duration-300">Submit</button>
            </div>
        </form>


        <% if (errors.length> 0) { %>
            <ul class="error">
                <% errors.forEach(function(error) { %>
                    <li>
                        <%= error.msg %>
                    </li>
                    <% }) %>
            </ul>
            <% } %>
    </main>
    <footer>
        <p>&copy; Copyright - Ramandeep Kaur</p>
    </footer>
</body>

</html>