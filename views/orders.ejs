<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Orders</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
    <header class="flex items-center justify-between p-4 bg-blue-500 text-white shadow-md">
        <h1 class="text-2xl font-bold">Throat-ease Drinks</h1>
        <nav class="flex space-x-6">
            <a href="/" class="hover:text-gray-200 transition duration-300">Order Juices</a>
            <a href="/orders" class="hover:text-gray-200 transition duration-300">View Orders</a>
            <a href="/login" class="hover:text-gray-200 transition duration-300">Login</a>
            <a href="/logout" class="hover:text-gray-200 transition duration-300">Logout</a>
        </nav>
    </header>
    <main class="max-w-4xl mx-auto bg-white p-6 rounded-lg shadow-lg">
        <h2 class="text-3xl font-semibold text-center text-blue-500 mb-6">View Orders</h2>

        <table class="min-w-full table-auto border-collapse text-left">
            <thead>
                <tr class="bg-gray-100">
                    <th class="px-4 py-2 text-gray-700">Name</th>
                    <th class="px-4 py-2 text-gray-700">Phone</th>
                    <th class="px-4 py-2 text-gray-700">Mango Juices</th>
                    <th class="px-4 py-2 text-gray-700">Berry Juices</th>
                    <th class="px-4 py-2 text-gray-700">Apple Juices</th>
                    <th class="px-4 py-2 text-gray-700">Total Cost</th>
                </tr>
            </thead>
            <tbody>
                <% orders.forEach((order)=> { %>
                    <tr class="border-b">
                        <td class="px-4 py-2 text-gray-700">
                            <%= order.name %>
                        </td>
                        <td class="px-4 py-2 text-gray-700">
                            <%= order.phone %>
                        </td>
                        <td class="px-4 py-2 text-gray-700">
                            <%= order.mangoJuices %>
                        </td>
                        <td class="px-4 py-2 text-gray-700">
                            <%= order.berryJuices %>
                        </td>
                        <td class="px-4 py-2 text-gray-700">
                            <%= order.appleJuices %>
                        </td>
                        <td class="px-4 py-2 text-gray-700">$<%= (order.mangoJuices * 2.99 + order.berryJuices * 1.99 +
                                order.appleJuices * 2.49).toFixed(2) %>
                        </td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>

        <div class="mt-6 text-center">
            <a href="/"
                class="inline-block px-6 py-3 bg-blue-500 text-white font-semibold rounded-lg hover:bg-blue-600 transition duration-300">
                Go Back to Order Page
            </a>
        </div>
    </main>

</body>

</html>